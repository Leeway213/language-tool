# language-tool

```shell
npm install language-tool -g
```
```shell
language-tool
Usage: language-tool <command>

Options:
  -V, --version  output the version number
  -h, --help     output usage information

Commands:
  pluck          pluck sentences
  split          split text file, support excel
  help [cmd]     display help for [cmd]
```
